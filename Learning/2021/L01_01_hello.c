/* gcw プログラミング講習会 */

/* 標準入出力ライブラリを導入 (これをしないと下のprintf関数を使うことができない) */
#include <stdio.h>

/* プログラムが実行されたときにこの関数が実行される */
int main(void){

  /* 文字列を表示 */
  printf("Hello World!");

  /* プログラムの終了 */
  return 0;
}
